#!/usr/bin/sh

SWIPL_DEB="swi-prolog_latest_arm.deb"
TMP="deb.$$"

#Download package
mkdir "$TMP" && \
   wget -P deb https://raw.githubusercontent.com/erlanger/swipl-termux/master/debs/$SWIPL_DEB

#Install package
dpkg -i "$TMP"/$SWIPL_DEB && rm -rf "$TMP"

